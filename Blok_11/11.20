import random
scores = []
for i in range(20):
    n1 = random.randint(2, 9)
    n2 = random.randint(2, 9)
    answer = input(f"What is the product of {n1} by {n2}? ")
    if int(answer) == n1 * n2:
        scores.append(1)
    else:
        scores.append(0)
print(scores)
