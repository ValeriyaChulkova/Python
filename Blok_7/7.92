# задаем список оценок
marks = [5, 4, 3, 2, 5, 4, 3, 5, 4, 3, 2, 5, 4, 3, 5, 4, 3, 2, 5, 4, 3, 5, 4, 3, 2, 5, 4, 3]

# флаг, который будет указывать, есть ли двойки в списке
has_two = False

# проходим по списку оценок
for mark in marks:
# если находим двойку, меняем значение флага и выходим из цикла
if mark == 2:
has_two = True
break

# проверяем значение флага и выводим соответствующее сообщение
if has_two:
print("В списке есть двойки")
else:
print("В списке нет двоек")
