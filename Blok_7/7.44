a = [1, 2, 3, ..., 20] # список целых чисел
sum = 0 # переменная для хранения суммы

for i in range(1, 65, 2): # перебираем каждое второе число от 1 до 64
sum += a[i-1] # добавляем в сумму элемент списка с индексом i-1

print(sum) # выводим результат
