a = float(input())
n = 2 # start with n = 2 since we know the sequence starts with 1 + 1/2
while True:
    sequence = [1 + 1/i for i in range(2, n+1)]
    if all(number >= a for number in sequence):
        print(n)
    n += 1
